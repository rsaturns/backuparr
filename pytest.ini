[pytest]
# tests/ has no __init__.py, so pytest's default import rootdir is tests/
# itself - without this, "import restore_actions" etc. (the app's
# top-level modules) fail with ModuleNotFoundError.
pythonpath = .
